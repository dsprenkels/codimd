apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codimd.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "codimd.name" . }}
    helm.sh/chart: {{ include "codimd.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "codimd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
